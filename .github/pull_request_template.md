## Summary

What this PR changes and why. Aim for 1-3 sentences.

## Test plan

- [ ] `npm test` passes locally
- [ ] `bash scripts/verify.sh` passes locally
- [ ] Manual smoke test (start server, hit `/api/health`, list presets) if server changed

## Changelog entry

Drop the bullet you'd add to `CHANGELOG.md` here. The release-spine will lift this into release notes.

```
- (added/changed/fixed) <one-line description>
```

## Breaking changes

- [ ] None
- [ ] Engine deterministic output changed (specify which preset/score)
- [ ] Public HTTP API shape changed (specify endpoint)
- [ ] Public TypeScript types changed (specify symbol)

## Related issues

Closes #
